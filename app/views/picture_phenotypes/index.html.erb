<div class="container test-phenotype__container">

  <h3 class="test-phenotype__title" >Listing all picture phenotypes</h3>
  <%if current_user%>
      <h4>
        <%=link_to " Add phenotype picture", {:action => "new"}, class: "btn btn-default test-phenotype__add-button" %>
      </h4>
  <%end%>
  <br/>
  <div class="table-responsive">
    <table class="table table-hover" id="no_snp_overview">
      <thead>
        <tr>
          <th>#</th>
          <th><%= sortable "id", "ID"%></th>
          <th><%= sortable "characteristic","Phenotype"%></th>
          <th><%= sortable "number_of_users","Number of users"%></th>
          <th><%= sortable "created_at", "Created"%>
        </tr>
      </thead>
      <% @phenotypes_paginate.each do |s| %>
      <tr>
        <td class="table-cell"><%= PicturePhenotype.all.sort { |a,b| a.id <=> b.id}.index(s) + 1%></td>
        <td class="table-cell"><%= s.id %></td>
        <td class="table-cell"><%= link_to(s.characteristic, s) %></td>
        <td class="table-cell"><%=s.number_of_users%></td>
        <td class="table-cell"><%=s.created_at%></td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
<%= page_navigation_links @phenotypes_paginate %>
