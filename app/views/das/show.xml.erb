<?xml version="1.0" standalone="no"?>
<DASGFF>
    <GFF version="1.01" href="http://www.ebi.ac.uk:/das-srv/easydas/das/23dns/features">
        <SEGMENT id="<%= @id %>" version="1.0" start="<%= @start %>" end="<%= @end %>">
            <% @user_snps.each do |us| %>
                <FEATURE id="<%=us.snp_name%>">
                    <TYPE id="<%= us.local_genotype %>" />
                    <METHOD id="" />
                    <START>
                        <%= Snp.find_by_name(us.snp_name).position %>
                    </START>
                    <END>
                        <%= Snp.find_by_name(us.snp_name).position %>
                    </END>
                    <SCORE>
                        0
                    </SCORE>
                    <PHASE>
                        -
                    </PHASE>
                    <ORIENTATION>
                        0
                    </ORIENTATION>
                </FEATURE>
            <% end %>
        </SEGMENT> 
    </GFF>
</DASGFF>
