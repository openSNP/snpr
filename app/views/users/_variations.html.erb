<table>
	<tr>
		<th>Characteristic</th>
		<th>Variation</th>
	</tr>
<% @phenotypes.each do |p| %>
	<tr>
		<td><%=p.characteristic%></td>
		<td><% if not p.variation.empty? %><%= p.variation%><% else %>Undescribed.<% end %></td>
	</tr>
<% end %>
</table>
