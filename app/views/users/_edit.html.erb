<% # helper thing so we don't have to put up fields into every page %>
<%= nested_form_for(@user) do |f| %>
<%= render 'shared/error_messages', :target => @user %>
<div class="field">
	<%= f.label :name %><br/>
	<%= f.text_field :name %>
</div>
<div class="field">
	<%= f.label :email %><br/>
	<%= f.text_field :email %>
</div>
<div class="field">
	<%= f.label :password %><br/>
	<%= f.password_field :password %>
</div>
<div class="field">
	<%= f.label :password_confirmation, "Confirmation" %><br/>
	<%= f.password_field :password_confirmation %>
</div>               

<h2>Phenotypes</h2>
	<%= f.fields_for :phenotypes do |p| %>
		 Characteristic: <%= p.text_field :characteristic %>
		 Variation: <%= p.text_field :variation%> <%= p.link_to_remove "Remove"%><br/>
 <% end %>
 <%= f.link_to_add "Add a phenotype", :phenotypes %>
<div class="actions">
	<%= f.submit %>
</div>
<% end %> 
