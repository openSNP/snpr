<div class="container test-message__container">
  <h3 class="test-message__title">Message</h3>
  <div class="test-message__message-informations">
    <h4>From: <%if @from != "Deleted User"%><%= link_to @from.name, @from %><%else%>Deleted User<%end%></h4>
    <h4>To: <% if @to == @current_user %> You <% else %> <% if @to != "Deleted User" %><%= link_to @to.name, @to %></h4><% else %>Deleted User<% end %><% end %></h4>
    <span class="test-message__message-date"><%= @message.created_at %> </span>

  </div>
  <div class="well test-message__message">
    <h3 class="test-message__message-subject">Subject: <%=@message.subject %></h3>
    <%=  simple_format(@message.body, class: "test-message__message-body") %>
  </div>
  <div class="test-message__buttons">
    <%if @from != "Deleted User"%>
    <%= link_to "Reply", {:controller => "messages", :action => "new", :user_to_send => @from.id, :message => @message}, :class => "btn btn-primary test-message__reply-button" %>
    <%end%>
    <%= button_to "Delete", message_path(@message.id), :method => :delete, :confirm => "Are you sure?", :class => "btn btn-danger test-message__delete-button"%>
  </div>
</div>
</div>
